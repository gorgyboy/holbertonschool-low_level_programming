#include<stdio.h>

/**
  * main - Prints msg
  *
  * Return: Returns 0
  */
int main(void)
{
	char msg[100] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s", msg);
	return (0);
}
