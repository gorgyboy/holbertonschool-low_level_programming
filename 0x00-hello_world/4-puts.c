#include <stdio.h>

/**
  * main - Prints message to stdout
  *
  * Return: Returns 0
  */
int main(void)
{
	char msg[62] = "\"Programming is like building a multilingual puzzle";

	puts(msg);
	return (0);
}
