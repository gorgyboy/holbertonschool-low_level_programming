#include "holberton.h"

/**
 * is_prime_number - check if a number is prime.
 * @n: number to check.
 *
 * Return: 1 prime number, otherwise return 0.
 */

int is_prime_number(int n)
{
	return (n);
}
